import { StyleSheet } from 'react-native';

import { WHATSAPP_COLOR } from '../../styles/colors';

const styles = StyleSheet.create({
  buttonRoundedWhatsapp: {
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: WHATSAPP_COLOR,
    borderRadius: 45,
    width: 45,
    height: 45,
    marginHorizontal: 7,
  },

});

export default styles;
