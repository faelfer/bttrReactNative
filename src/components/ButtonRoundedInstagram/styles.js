import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonGradientInstagram: {
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
    height: '100%',
    width: '100%',
    borderRadius: 25,
  },

  buttonRoundedInstagram: {
    alignItems: 'center',
    justifyContent: 'center',
    width: 45,
    height: 45,
    marginHorizontal: 7,
  },

});

export default styles;
