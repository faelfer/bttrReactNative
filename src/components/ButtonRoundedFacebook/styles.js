import { StyleSheet } from 'react-native';

import {
  FACEBOOK_COLOR,
} from '../../styles/colors';

const styles = StyleSheet.create({
  buttonRoundedFacebook: {
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: FACEBOOK_COLOR,
    borderRadius: 45,
    width: 45,
    height: 45,
    marginHorizontal: 7,
  },

});

export default styles;
